// All the magic goes here

import 'bootstrap';

import GLightbox from 'glightbox';

const lightbox = GLightbox({
    touchNavigation: true,
    loop: true,
    zoomable: true,
    openEffect:"fade"
});
